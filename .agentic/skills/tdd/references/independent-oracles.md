# Independent expected values

A test cannot prove an algorithm if it computes the expected value with the same algorithm. Use constants from the specification, a simpler independent formulation, a trusted external fixture, metamorphic properties, or cross-implementation comparison.
