# Validation — UPG-001

Review the contract, exact diff, RED/GREEN evidence, and dry-run filesystem
hashes. Pass only if preview is deterministic, argument behavior is unambiguous,
and no planning helper mutates the target.
